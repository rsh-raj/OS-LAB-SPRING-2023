max_degreee = 9458 avg_degree = 15 median = 6 max_no_of_action = 10340
    // size_t ans = 0,max_action=0;
    // vector<int> degree;
    // for (auto node : graph)
    // {
    //     ans = node.second.neighbors.size();
    //     degree.push_back(ans);
    // }
    // sort(degree.begin(), degree.end());
    // for(int i=degree.size()-1;i>=degree.size()-100;i--) max_action+=(k+log2(degree[i]));
    // cout <<max_action<< endl;
    // cout << (degree[graph.size() / 2] + degree[graph.size() / 2 + 1]) / 2;